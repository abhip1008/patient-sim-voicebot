# Pipecat with the service extras we use:
#   deepgram = STT, openai = patient LLM, cartesia = TTS, silero = VAD
pipecat-ai[deepgram,openai,cartesia,silero]==1.4.0

# Web server hosting the TwiML route + Twilio Media Stream WebSocket
fastapi
uvicorn[standard]

# Twilio REST SDK (places the outbound call)
twilio

# Config + audio post-processing
python-dotenv
pydub

# CA bundle for TLS verification (used explicitly in src/config.py to fix
# "CERTIFICATE_VERIFY_FAILED" in venvs that can't see the system CA store)
certifi
