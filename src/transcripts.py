"""Writes labeled, timestamped transcripts (.txt + .json) per call.

Implemented in PHASE 5. Speaker labels (Patient / Agent), time relative to call start.
"""

# TODO(Phase 5): assemble both sides (our text + STT of agent), keep time-aligned.
