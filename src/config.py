"""Central settings: loads + validates env vars at startup.

Implemented in PHASE 1. Will read .env via python-dotenv and fail loudly
with a clear message if any required variable is missing.
"""

# TODO(Phase 1): load .env, validate required keys, expose a Settings object.
