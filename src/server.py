"""FastAPI app: the TwiML route + the WebSocket Twilio Media Stream connects to.

Implemented in PHASE 2 (telephony path) and wired to the Pipecat pipeline in PHASE 3.
"""

# TODO(Phase 2): POST /twiml returns TwiML (stream + record); WS /ws accepts audio.
