"""Places the outbound call to TARGET_NUMBER via the Twilio REST API.

Implemented in PHASE 2. CLI: python -m src.call --scenario <id>
"""

# TODO(Phase 2): use Twilio REST client to dial TARGET_NUMBER, point at /twiml.
