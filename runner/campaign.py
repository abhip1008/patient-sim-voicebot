"""Orchestrates a batch of N calls across scenarios (the 10+ call campaign).

Implemented in PHASE 8. Runs scenarios sequentially, saves recording + transcript +
metadata per call; supports the wave-1 / improve / wave-2 iteration loop.
"""

# TODO(Phase 8): sequential runner with gaps, metadata logging, cost logging.
